The file `/workspace/hello.py` exists and contains `print("Hello, world!")`, which when executed with Python prints exactly `Hello, world!` (print adds a trailing newline, which is standard/expected and doesn't violate "exactly"). This fully satisfies the spec.

PASS

No issues found. `hello.py` correctly and minimally implements the spec.